{% extends 'base.html.twig' %}

{% block title %}Mon Profil{% endblock %}
{%  block titrePage %}Mon Profil{% endblock %}

{% block body %}

    {{ form_start(editForm) }}

    {{ form_row(editForm.username, {label: 'Pseudo'}) }}
    {{ form_row(editForm.firstName, {label: 'Prénom'} ,{'attr': required}) }}
    {{ form_row(editForm.name, {label: 'Nom'}) }}
    {{ form_row(editForm.phone, {label: 'Téléphone'}) }}
    {{ form_row(editForm.email, {label: 'Email'})}}
    {{ form_row(editForm.campus, {label: 'Campus'}) }}
    <br>
    <div class="text-center">
    <button type="submit" class="btn btn-secondary mx-2">Enregistrer</button>
     <a href="{{ path('home_welcome')}}"><button class="btn btn-secondary mx-2">Annuler</button></a>
    </div>
    {{ form_end(editForm) }}




{% endblock %}
