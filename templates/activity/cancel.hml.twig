{% extends 'base.html.twig' %}

{% block title %}Annuler une sortie{% endblock %}

{%  block titrePage %}Annuler une sortie{% endblock %}

{% block body %}
    {{ form_start(cancelFormActivity) }}
    {{ form_row(cancelFormActivity.name, {label: 'Nom de la sortie'}) }}
    {{ form_row(cancelFormActivity.beginDateTime, {label: 'Date de la sortie'}) }}
    {{ form_row(cancelFormActivity.campus, {label: 'Campus'}) }}
    {{ form_row(cancelFormActivity.location, {label: 'Lieu'}) }}
    {{ form_row(cancelFormActivity.cancellationReason, {label: 'Motif'}) }}
    <br>
    <div class="text-center">
        <button type="submit" class="btn btn-secondary">Enregistrer</button>
        <a href="#"><button class="btn btn-secondary mx-2">Annuler</button></a>
    </div>
    {{ form_end(cancelFormActivity) }}
{% endblock %}